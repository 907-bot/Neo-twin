# NeoTwin Backend Dependencies
# Deployed on HuggingFace Spaces (CPU tier by default)
# For GPU features (gsplat, torch), upgrade to a GPU Space

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# --- Config & Utils ---
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1
psutil>=6.0.0

# --- AI / Gemini ---
google-generativeai==0.8.0

# --- Image / Video Processing ---
Pillow==10.4.0
numpy>=1.24.0
opencv-python-headless==4.10.0.84
ffmpeg-python==0.2.0

torch==2.4.0
torchvision==0.19.0
transformers==4.44.2
clip @ git+https://github.com/openai/CLIP.git

# --- Optional: uncomment for GPU Space ---
# torch==2.6.0
# torchvision==0.21.0
# gsplat>=1.3.1
